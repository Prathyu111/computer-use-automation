# Curated public evidence

Six reviewer runs. Full policy (screenshot omission, synthetic mock data, public snapshot vs canonical capability): `../README.md`.

Do not replace `discover-f83a7518/capability.json` with `capabilities/local.mock_core.lookup_savings_balance.v1.0.0.json`.
