# Curated public evidence

Seven reviewer runs. Current manual HITL proof is `replay-a46f4e20` (includes operator-reported `human_action`). `replay-df9e8123` is historical HITL without `human_action` and is not rewritten. Full policy (screenshot omission, synthetic mock data, public snapshot vs canonical capability): `../README.md`.

Do not replace `discover-f83a7518/capability.json` with `capabilities/local.mock_core.lookup_savings_balance.v1.0.0.json`.
