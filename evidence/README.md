Evidence from discovery and replay runs lands in `evidence/<run-id>/`. Checked-in samples:

- `examples/replay_success` — `memberId=12345`
- `examples/replay_not_found` — business outcome, not a crash

A genuine LLM discovery log belongs here after `python -m cua discover --start-mock --goal "..."`.

